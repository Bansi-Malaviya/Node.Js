<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogging</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <style>
        body {
            font-family: 'Arial', sans-serif;
            color: #4a4a4a;
            background-color: #f5ebe0;
        }

        h2{
            color: #795548;
            font-weight: 600;
        }
        .container {
            max-width: 500px;
            margin-top: 50px;
        }

        .card {
            background-color: #EFEBE9;
            /* Soft beige card */
            color: #4E342E;
            /* Medium brown text */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }

        .form-label {
            color: #5D4037;
            /* Warm brown labels */
        }

        .form-control {
            background-color: #ffffff;
            /* Light cream input */
            border: 1px solid #BCAAA4;
            /* Subtle brown border */
            color: #4E342E;
        }

        .form-control:focus {
            background-color: #FFFFFF;
            color: #4E342E;
            box-shadow: none;
            border-color: #8D6E63;
        }

        .btn {
            background-color: #795548;
            /* Light chocolate button */
            border: none;
            color: white;
        }

        .btn:hover {
            background-color: #795548dc;
            /* Darker chocolate on hover */
            color: #ffffff;
        }

        
    </style>
</head>
<body>

    <div class="container">
        <h2 class="text-center my-4">Edit Your Blog</h2>

        <div class="card">
            <form action="/updateblogdata" method="post" enctype="multipart/form-data">
                <input type="hidden" name="editId" value="<%= oneRow?.id %>">

                <div class="mb-3">
                    <label class="form-label">Title:</label>
                    <input type="text" name="title" class="form-control" value="<%= oneRow?.title %>" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Description:</label>
                    <textarea name="description" class="form-control" rows="4" required><%= oneRow?.description %></textarea>
                </div>

                <div class="mb-3">
                    <label class="form-label">Upload New Image:</label>
                    <input type="file" name="image" class="form-control" accept="image/*">
                    <img src="<%= oneRow?.image %>" width="100" class="image-preview" alt="Current Image">
                </div>

               <div align="center">
                    <button type="submit" class="btn w-50 m">Update Blog</button>
               </div>
            </form>
        </div>

        <div class="text-center my-3">
            <a href="/viewblogpage" class="btn btn-secondary">View Blogs</a>
        </div>
    </div>

</body>
</html>
